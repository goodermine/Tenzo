{
  "name": "greyline",
  "version": "0.1.0",
  "private": true,
  "description": "A browser first-person shooter built on three.js with fully procedural geometry, textures and audio.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --minify --outfile=dist/greyline.js --log-level=warning",
    "dev": "esbuild src/main.js --bundle --format=iife --outfile=dist/greyline.js --sourcemap --watch"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
